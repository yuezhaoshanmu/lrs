PORT=4000
JWT_SECRET=change-me
CLIENT_ORIGIN=http://localhost:5173
# Supabase REST endpoint (the project URL may include /rest/v1; both forms work)
SUPABASE_URL=https://dwiepfpenidfmbumisxo.supabase.co/rest/v1/
# Keep this server-side. Prefer the service role key for the private tables.
SUPABASE_SERVICE_ROLE_KEY=
# Public anon key is used only to validate Auth tokens when a service key is absent.
SUPABASE_ANON_KEY=
# Only enable when migrating the old bigint mirror schema. Leave disabled for Auth UUID tables.
SUPABASE_LEGACY_SYNC=false
